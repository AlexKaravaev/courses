\RequirePackage[utf8]{inputenc}
\RequirePackage[T2A]{fontenc}
\RequirePackage[russian]{babel}  

% требуется для оформления ссылок
\RequirePackage[pdftex,unicode,colorlinks,linkcolor=blue,citecolor=red,urlcolor=blue,bookmarksopen,pdfhighlight=/N]{hyperref}

% отвечают за набор формул и др. мат. конструкций
\RequirePackage{amscd,amsmath,amssymb,amsfonts,textcomp,latexsym,pb-diagram,euscript}

% помогают при наборе таблиц
%\RequirePackage{longtable, array, colortbl}

% цвета, рисунки и пр.
\RequirePackage[pdftex]{graphicx}
%\RequirePackage{floatrow,floatflt}
\RequirePackage{wrapfig}

% нужен для замены импортного "рис:" на наше "рис."
\RequirePackage[labelsep=period]{caption}

% настраиваем страничку
\RequirePackage[a4paper,left=15mm,right=15mm,top=15mm,bottom=15mm,headsep=5mm,headheight=7mm]{geometry}

% а без этого колонтитулы не получатся
%\RequirePackage{fancyhdr}

% ну и всякие мелочи: абзацные отступы, междустрочные интервалы...
%\RequirePackage{indentfirst}
%\RequirePackage{setspace}
%\singlespacing
\parindent=5mm
\renewcommand{\baselinestretch}{1,3}
\raggedbottom
\parskip=0mm
\setcounter{secnumdepth}{10}
%\righthyphenmin=3
%\renewcommand{\labelitemi}{$\diamond$}
\sloppy
\clubpenalty=10000
\widowpenalty=10000

%Переназначение шрифтов
%\tiny \scriptsize \footnotesize \small \normalsize \large \Large \LARGE \huge \Huge
%\renewcommand{\huge}{\fontsize{25}{25}\selectfont} %Используется в part
\renewcommand{\Large}{\fontsize{14}{14}\selectfont} %Используется в section
%\renewcommand{\normalsize}{\fontsize{12}{12}\selectfont} %Используется для текста

%Многоуровневые нумерованные списки
\renewcommand{\theenumi}{\arabic{enumi}}
\renewcommand{\theenumii}{\arabic{enumii}}
\renewcommand{\theenumiii}{\arabic{enumiii}}
\renewcommand{\theenumiv}{\arabic{enumiv}}

\renewcommand{\labelenumi}{\theenumi}
\renewcommand{\labelenumii}{\theenumi.\theenumii}
\renewcommand{\labelenumiii}{\theenumi.\theenumii.\theenumiii}
\renewcommand{\labelenumiv}{\theenumi.\theenumii.\theenumiii.\theenumiv}

%отключить переносы
\hyphenpenalty=10000
%в том числе и в формулах
\binoppenalty=10000
\relpenalty=10000
 
%Устанавливает возможность растяжения строк
%\tolerance=500

